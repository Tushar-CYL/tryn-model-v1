# video_model

Reuses image_model encoder + temporal module (frame sampling, keyframe selection, token merging/Q-Former). Video → meaning. §9 Phase 4. Efficiency work concentrates here (token explosion).
